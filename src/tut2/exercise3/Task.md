# Task Description

Given the classes [Piece](Piece.java), [Tile](Tile.java) and
the enum [Type](Type.java), implement the following method under
[Main](Main.java):

```java
void printRooks(Tile[][] tiles) {
    // TODO: Implement me
}
```

The method `printRooks` should:

- Iterate through the chess board
- For each `Type.ROOK` on board, print the position of the piece
- Output the following format: `(x, y)`
